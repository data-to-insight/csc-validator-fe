import styled from "@emotion/styled";

const Version = styled.div`
  position: relative;
  width: 100%;
  text-align: center;
  bottom: 0;
  right: 0;

  a {
    color: black;
  }
`;

export { Version };
