import styled from "@emotion/styled";
import { greys } from "@sfdl/sf-mui-components";

const Version = styled.div`
  position: relative;
  width: 100%;
  text-align: center;
  bottom: 0;
  right: 0;
  color: ${greys.mid};

  a {
    color: ${greys.mid};
  }
`;

export { Version };
