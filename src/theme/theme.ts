import { typography } from "./typography";
import { colors } from "./colors";

export const theme = {
  palette: {
    primary: {
      main: colors.primary,
    },
  },
  typography: {
    fontFamily: typography,
  },
};
